<!DOCTYPE html>
<html lang="en">
<%- include('partials/header')%>
<body>
    <%- include('partials/navbar')%>
    <button class = 'primary-btn create-world-btn'>Create New World</button>
    <div class="grid-wrapper">
        <div class="worlds-grid">
            <% worlds.forEach((world) => { %>
                <a href = "/worlds/<%= world.id %>">
                    <div class = 'world-box'>
                        <h1><%= world.name %></h1>
                    </div>
                </a>
            <% }) %>
        </div>
    </div>
</body>
</html>